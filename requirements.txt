# ============================================================
# Tech Challenge Fase 4 — dependências
# Instale com:  pip install -r requirements.txt
# Python 3.10+ recomendado
# ============================================================

# ----- Base / comum a todas as entregas -----
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
python-dotenv>=1.0

# ----- Entrega 1: Análise de Vídeo (OpenPose) -----
# O OpenPose é um binário EXTERNO (não instala via pip) — ver docs/openpose_setup.md.
# Nosso código só processa os keypoints JSON que ele gera; as libs abaixo bastam.
# (numpy/pandas/matplotlib já estão na seção base.)
opencv-python>=4.9        # metadados do vídeo (fps) e desenho do overlay
imageio-ffmpeg>=0.4      # ffmpeg embutido: transcodifica o overlay p/ H.264 (tocável no navegador)
tabulate>=0.9            # tabelas Markdown no relatório (DataFrame.to_markdown)

# ----- Entrega 2: Análise de Áudio (AWS) -----
# Provedor de nuvem: AWS (Amazon Transcribe + Comprehend Medical).
# A Azure foi descartada por indisponibilidade de cota — ver o relatório técnico.
boto3>=1.34                            # Transcribe (transcrição) + Comprehend Medical (entidades)
librosa>=0.10                          # biomarcadores acústicos
soundfile>=0.12

# ----- Entrega 3: Detecção de Anomalias -----
# usa numpy/pandas/scikit-learn já listados acima

# ----- Interface de demonstração (Entrega 1) -----
gradio>=5.0              # app web local p/ o vídeo-demo (src/video/app.py);
                         # >=5.0 por causa do show_progress_on em app.py

# ----- Notebooks / exploração -----
jupyter>=1.0
ipykernel>=6.29

# ----- Testes -----
pytest>=8.0
